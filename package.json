{
  "name": "html-to-pdf",
  "private": true,
  "scripts": {
    "start": "parcel index.html --open",
    "build": "rimraf ./dist && parcel build index.html --no-minify --no-source-maps --public-url ."
  },
  "dependencies": {
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.2"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-airbnb-base": "^14.1.0",
    "eslint-plugin-import": "^2.20.2",
    "sass": "^1.26.5"
  }
}
